# Command Linux 
## symbolic link
```bash
  ln -s source destanation
```
# folder or file is source is not in destenation
* sample: ```bash ln -s /mnt/archive /home/user/ ```
* after run command exsic folder archive in /home/user/
* if remove source is error in destanation
**  if create source to destanation by aother name ```bash ln -s /mnt/archive /home/user/monitor ```
